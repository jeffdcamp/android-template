name: PR CI

on:
  pull_request:
    branches: [ master ]

concurrency:
  group: pr-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit
    with:
      variant: Alpha
      detektTask: detektDebug
