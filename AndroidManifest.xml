<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="100" android:versionName="1.0.0.0-SNAPSHOT" package="org.company.project">
<!--	android:versionCode="${parsedVersion.majorVersion}${parsedVersion.minorVersion}${parsedVersion.incrementalVersion}"
        android:versionName="${project.version}"-->
    <uses-sdk android:minSdkVersion="3" android:targetSdkVersion="15"/>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/> <!-- cache to sdcard -->
    <uses-permission android:name="android.permission.READ_LOGS"/>
    
    <!-- MyApplication -->
    <application android:debuggable="true" android:icon="@drawable/app_icon" android:label="@string/app_name" android:name=".MyApplication">
        <!-- Activities -->
        <activity android:icon="@drawable/app_icon" android:name=".activity.MainActivity" android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <activity android:name=".activity.AboutActivity"/>

        <activity android:label="@string/pref_title" android:name=".activity.SettingsActivity"/>
    </application>
</manifest>